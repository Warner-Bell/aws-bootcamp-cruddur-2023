#! /usr/bin/bash

ABS_PATH=$(readlink -f "$0")
SERVERLESS_PATH=$(dirname $ABS_PATH)
WARF_FILE_PATH="$SERVERLESS_PATH/files/Warf.jpg"

aws s3 cp "$WARF_FILE_PATH" "s3://assets.$DOMAIN_NAME/avatars/original/Warf.jpg"