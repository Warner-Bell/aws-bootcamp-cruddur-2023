#! /usr/bin/bash

ABS_PATH=$(readlink -f "$0")
SERVERLESS_PATH=$(dirname $ABS_PATH)
WORF_FILE_PATH="$SERVERLESS_PATH/files/Worf.jpg"

aws s3 cp "$WORF_FILE_PATH" "s3://gadgetoolslabscruddur-uploaded-avatars/Worf.jpg"